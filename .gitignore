# --- renv (project-local package library; like .venv) ---
# Commit renv.lock but NOT the installed library.
renv/library/
renv/local/
renv/cellar/
renv/lock/
renv/python/
renv/staging/
renv/sandbox/

# --- R session/history cruft ---
.Rhistory
.Rapp.history
.RData
.Ruserdata
*.Rproj.user/
.Rproj.user

# --- secrets: NEVER commit the Comtrade API key ---
.Renviron
.env

# --- large raw Comtrade pulls we don't use in the main analysis ---
# (us_eu, us_canada, china_aus only used in early HS2 multi-pair work)
# Re-pull via 01_pull.R if needed.
data_raw/us_eu.rds
data_raw/us_canada.rds
data_raw/china_aus.rds

# --- data_processed RDS intermediates (regenerable by running scripts) ---
data_processed/*.rds

# --- figures: TRACK all PNGs ---
# Figures are deliverables; tracking them makes the repo self-contained

# --- outputs: TRACK all documents ---
# FINDINGS.md, EXECUTIVE_SUMMARY.md etc. are authored, not generated

# --- OS noise ---
.DS_Store
Thumbs.db
.positai
